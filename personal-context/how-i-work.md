# How I Work Best

Distilled, validated insights about how you operate effectively. Add an entry when a pattern shows up across 3+ sessions or you explicitly confirm a non-obvious approach worked. Corrections go in `auto-memory/` feedback files, not here.

Each entry: short title, the insight in one sentence, why it works, the trigger / when it applies.

---

## Entries

### <pattern title>

- **Insight:** `<one sentence>`
- **Why it works:** `<mechanism>`
- **Trigger / when it applies:** `<when agents should apply this>`
