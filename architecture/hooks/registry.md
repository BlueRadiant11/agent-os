---
name: Hook registry
description: Active hooks in ~/.claude/settings.json `"hooks"` block. Each hook script lives in ~/.claude/hooks/.
type: registry
canonical: false
owner: chief-of-staff
last_verified: 2026-06-10
---

# Hook registry — `~/.claude/settings.json` `"hooks"` field

**Removed 2026-06-10 (sentinel teardown, Fable-fit overhaul):** `pre-tool-use-bridge-only.sh` PreToolUse:Agent hook. Enforced the `BRIDGE_PAD_PATH:` sentinel — the last remnant of the bridge protocol after the 2026-05-19 pad-layer teardown removed everything the sentinel pointed at. Post-teardown the sentinel gated nothing (its own docs: "value carries no semantics") while blocking native multi-agent skill flows that don't know the convention. Script archived to `~/.claude/hooks/.archive-2026-06-10-pre-tool-use-bridge-only.sh` (+ test harness); bridge-dispatch skill archived to `~/.claude/.archive/skills/bridge-dispatch-2026-06-10/`; bridge-protocol.md archived to `~/.claude/architecture/agents/.archive-2026-06-10-bridge-protocol.md`. The design-question fallback (former Item 3) survives — canonical home now `~/.claude/skills/dispatch-protocol/SKILL.md` § "Design-question fallback". Receipt: `~/personal-context/decisions/agent-os/2026-06-10-fable-fit-overhaul.md`.

**Removed 2026-06-10 (SessionStart slim, Fable-fit overhaul):** `personal-context-archindex.sh` (hook 7/10, 17KB output) and `personal-context-portfolio.sh` (hook 10/10, 16KB output) SessionStart hooks. Both overflowed the per-hook inline budget every session, so the "persisted-output failsafe" had become the main path — the content wasn't actually auto-loading. Both files already had working on-demand paths (CLAUDE.md "read index.md first" rule + context-router keyword injection). `personal-context-runtime.sh` now emits one-line pointers to both. Scripts archived to `~/.claude/hooks/.archive-2026-06-10-personal-context-{archindex,portfolio}.sh`. Saves ~33KB per session start. Receipt: `~/personal-context/decisions/agent-os/2026-06-10-fable-fit-overhaul.md`.

**Removed 2026-05-19 (pad-layer teardown):** `bridge-protocol-check.sh` PostToolUse:Agent and `bridge-surface.py` UserPromptSubmit hooks. Both were part of the heavyweight bridge protocol's audit-trail infrastructure (validating pad has `return.md`, polling active pads for completions). Pad layer torn out per `~/personal-context/decisions/agent-os/2026-05-19-bridge-protocol-simplification.md` — these two hooks lose their referent. Scripts archived to `~/.claude/hooks/.archive-2026-05-19-*.{sh,py}` in case of reversal. Replaced by Claude Code's native run_in_background completion notification.

**Removed 2026-05-19 (dead-weight cleanup):** `cron-catchup-notice.sh` SessionStart hook. Walked the runner registry at `~/.claude/scripts/runners.conf` on every session start; registry has been empty (all comments, zero active runners) since cron-runaway 2026-05-04. Hook was firing with no work to do. Archived to `~/.claude/hooks/.archive-2026-05-19-cron-catchup-notice.sh`. The launchd runner lane itself was retired 2026-06-10 when the weekly sweeps moved to native CronCreate scheduling — see `~/.claude/architecture/runners/active.md`.

## Active hooks (16)

| Event | Matcher | Script | Purpose |
| --- | --- | --- | --- |
| `PostToolUse` | `Edit\|Write\|MultiEdit` | `~/.claude/hooks/settings-mirror-reminder.sh` | When `~/.claude/settings.json` is the edited file, emit `systemMessage` + `additionalContext` reminding Chief of Staff to mirror the change in `~/.claude/architecture/permissions/` per the source-of-truth rule. Silent on every other path. Built 2026-05-02. |
| `PostToolUse` | `Edit\|Write\|MultiEdit` | `~/.claude/hooks/single-page-rule.sh` | When a file under `~/personal-context/**.md` is edited and exceeds 100 lines, emit `systemMessage` + `additionalContext` flagging the violation against the single-page rule canonically defined in `memory/schema.md`. Silent on under-cap files and non-personal-context paths. Built 2026-05-02. |
| `PostToolUse` | `Edit\|Write\|MultiEdit` | `~/.claude/hooks/hook-registry-mirror-reminder.sh` | When `~/.claude/settings.json` is the edited file, count hook entries in the post-edit `hooks` block and compare to active-hooks rows in this registry. If settings.json count exceeds registry count, emit `systemMessage` + `additionalContext` reminding to mirror the new hook here per the rule at `agents/arch-implementer.md:146`. Silent on counts-equal and on non-matching paths. Built 2026-05-02. |
| `PostToolUse` | `Edit\|Write\|MultiEdit` | `~/.claude/hooks/canonical-edit-threshold.sh` | Per-session counter for canonical-file edits. Reads post-edit file's frontmatter (first 20 lines); if `canonical: true`, increments `~/.claude/state/canonical-edit-counter-<session_id>.json`. At threshold 3, emits `additionalContext` reminding to dispatch `defrag-agent` before session end. Suppresses emission on edits 4+ to avoid spam. Fail-safe: any parse/IO error → exit 0. Enforces Pass B Signal 9 (`~/.claude/agents/defrag-agent.md:145`) — load-bearing detection signal previously documented with no enforcement (Signal-A shape). Built 2026-05-12 by arch-implementer from defrag-agent HK-01 finding. |
| `PostToolUse` | `Write\|MultiEdit` | `~/.claude/hooks/post-write-verify.sh` | Verifies that Write/MultiEdit tool calls actually landed on disk. Reads `tool_input.file_path` from stdin JSON; if the file does not exist post-tool-call, emits `additionalContext` warning of silent FS failure (likely macOS TCC denial on Desktop/Documents/Downloads guarded paths). Silent on success. Built 2026-05-12 by arch-implementer from CoS-surfaced HK-02 finding after observed silent-write failure to `~/Desktop/hooks-audit.md`. Closes a tool-layer truth-above-all gap: the Write tool reports `File created successfully` based on upstream API success without `stat`ing post-write. |
| `PostToolUse` | `Agent` | `~/.claude/hooks/memory-agent-handoff-check.sh` | Fires after every Agent tool call. Filters by `subagent_type == "memory-agent"`. Parses the agent's response and verifies all seven mandatory phase rows (PHASE 0 through PHASE 6) are present in the response body. Missing rows trigger `additionalContext` reminding the parent agent that the memory-agent handoff is incomplete. Trivial-run carve-out: if the response has zero `PHASE N:` markers, exit silently (likely a "where does X go" answer, not a full sweep). Built 2026-05-12 as enforcement layer for the phased operating loop locked in `~/.claude/agents/memory-agent.md` and `~/.claude/architecture/memory/agent-charter.md`. Bridge-mode gate (added 2026-05-17) became dead code 2026-05-19 when the pad layer was removed; the dead branch was stripped from the script 2026-06-10. Decision: `~/personal-context/decisions/agent-os/2026-05-12-memory-agent-phased-loop.md`. |
| `SessionStart` | (no matcher) | `~/.claude/hooks/personal-context-identity.sh` | Hook 1/8 of the split SessionStart loader. Emits identity.md only. Always-on canon. Built 2026-05-03 (Unit 1 of CLAUDE.md coherence pass; iterated to 10-hook split after 4-hook split still overflowed; slimmed to 8 hooks 2026-06-10). ~5KB output. |
| `SessionStart` | (no matcher) | `~/.claude/hooks/personal-context-decisions.sh` | Hook 2/8. Emits decision-log.md (life-level decision log). Always-on canon. Built 2026-05-03. ~9KB output. |
| `SessionStart` | (no matcher) | `~/.claude/hooks/personal-context-howiwork.sh` | Hook 3/8. Emits how-i-work.md (validated working-style insights). Always-on canon. Built 2026-05-03. ~7KB output. |
| `SessionStart` | (no matcher) | `~/.claude/hooks/personal-context-voice.sh` | Hook 4/8. Emits work-style/voice-canon.md + hard-rules.md. Always-on canon. Built 2026-05-03. ~8.5KB output. |
| `SessionStart` | (no matcher) | `~/.claude/hooks/personal-context-process.sh` | Hook 5/8. Emits work-style/{format-and-reports,coding-workflow,agent-governance}.md. Always-on canon. Built 2026-05-03. ~7KB output. |
| `SessionStart` | (no matcher) | `~/.claude/hooks/personal-context-life.sh` | Hook 6/8. Emits work-style/{schedule-and-habits,spending-and-preferences,external-writing}.md. Always-on canon. Built 2026-05-03. ~5KB output. |
| `SessionStart` | (no matcher) | `~/.claude/hooks/personal-context-shelf.sh` | Hook 7/8. Emits shelf.md (paused items index). Always-on canon. Built 2026-05-03. ~5.5KB output. |
| `SessionStart` | (no matcher) | `~/.claude/hooks/personal-context-runtime.sh` | Hook 8/8. Emits runtime state — inbox enumeration, permission counts, recent decision activity, conditional Agent OS extras, plus on-demand pointers to architecture/index.md and the portfolio map (added 2026-06-10 when hooks 7+10 of the old split were demoted). Refreshed each session start. Built 2026-05-03. ~5KB output. |
| `UserPromptSubmit` | (no matcher) | `~/.claude/hooks/bedtime-lockout.sh` | Enforces an example bedtime lockout. Window: 20:30–06:00 local. Outside window: silent exit. Inside window: returns `{"decision":"block","reason":...}` to halt prompt processing per Claude Code hook spec. Example of a deterministic-friction hook — enforce a personal protocol at the harness layer instead of relying on discretion. Override path: edit `~/.claude/settings.json` or this script (deliberate, visible, leaves a diff). Wired ahead of `context-router.py` in the UserPromptSubmit chain so it short-circuits before any context injection runs. |
| `UserPromptSubmit` | (no matcher) | `~/.claude/hooks/context-router.py` | Keyword-routed on-demand context loader. Reads the user prompt from stdin, matches against the keyword map at `~/.claude/hooks/context-router-map.json`, and injects matching `.context/*.md` (cwd-scoped, e.g., Project Alpha when `cwd ⊆ ~/Desktop/project-alpha/`) and personal-context files (always-available) as `additionalContext`. Two-tier routing: `always` entries trigger from any cwd (project files in `~/personal-context/projects/`, key decision files); `cwd_scoped` entries trigger only when cwd is inside the named scope. Word-boundary case-insensitive regex matching. 20KB total budget per invocation, 8KB per file. Files with more keyword matches win when budget is tight. Fail-safe: any error path → exit 0 silently, never blocks the prompt. Built 2026-05-09 to address the soft-guidance enforcement gap in proactive-context routing — replaces the "agent must remember to read CLAUDE.md's routing table" pattern with deterministic keyword-triggered injection. |

**Removed 2026-05-04:** `agent-cron-catchup.sh` (auto-dispatch design). Fanned out to 3,060 dispatches in ~5 hours on its first morning because stamp-after-success + fire-and-forget background dispatch is not real idempotency, and the hook fired inside the very subprocesses it spawned. Replaced 2026-05-04 by `cron-catchup-notice.sh` (notice-only; itself removed 2026-05-19). Full post-mortem: `~/personal-context/decisions/agent-os/2026-05-04-cron-runaway-disabled.md`.

## Convention

Hook scripts live in `~/.claude/hooks/<name>.sh` (new convention as of 2026-05-02 — script files keep hook logic inspectable instead of buried inline in settings.json one-liners).

Permission rules for `~/.claude/hooks/**` live in `permissions/allow.md` (Hooks CRUD row).
